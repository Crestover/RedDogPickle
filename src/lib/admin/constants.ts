/**
 * Admin panel path — deliberately non-obvious (defense-in-depth alongside
 * the password gate, not a substitute for it). Not linked from anywhere in
 * the public-facing app.
 */
export const ADMIN_BASE_PATH = "/rd-admin-7k3m9";
export const ADMIN_LOGIN_PATH = `${ADMIN_BASE_PATH}/login`;
